# Changelog 5/07 — Response & Remediation Status

_Prepared 2026-07-06. Status of every item Luca raised in the 5/07 testing pass, what was changed, how it was verified, and what's still open. Shipped as PR #206 (merged `fde18be`, deployed to prod `6a4ba8c3` on 2026-07-06; PR #204 merged first as its dependency, migration 030 applied to prod before deploy)._

**Legend:** ✅ done & live-validated · 🟩 fixed in code (tsc-clean + tests) · 🟨 partial · 🟥 open · ❓ needs your input

---

## 1. Status of every item

| # | Item (5/07) | Status | What was done / evidence |
|---|---|---|---|
| 1 | Italian works intermittently | ✅ | Root cause found: **skills were the one surface never localized** — `runSkill` ignored the project locale, so every analysis (scoring, market research, …) streamed English into Italian projects while chat/watchers were correctly Italian. Now `SKILL.it.md` is loaded for Italian projects (all 20 skills have one) + an explicit language directive; Italian scorecard parsing hardened (accented dimension names like "Opportunità di Mercato" no longer drop). Verified: Italian fixtures in `score-summary.test.ts` / `skill-output.test.ts`; full Playwright pass on an Italian project — chrome, graph, Today all Italian. |
| 2 | Home / Co-Pilot often fail to load | ✅ | Two proven failure paths: the project-load error was **sticky for the whole session** (one transient 5xx → error until reload), and an empty stage evaluation **crashed the whole page** into the generic "Failed to load project" screen. Now: automatic retries with backoff (transient errors only), a working **Retry** button on the error screen, each Home panel isolated in its own error boundary (one bad panel degrades to a muted card instead of killing the page), the actions API no longer 500s everything when one query hiccups, and bodyless DELETEs no longer 415. Verified live: Retry recovers after a failed load; Today renders with zero boundary errors. |
| 3.1 | Scoring not proposed after doc-upload canvas | ✅ | State-driven nudge: canvas core complete AND no score row → the co-pilot's direction engine leads with "run the initial scoring" **and** the opening brief after doc-upload prepends a one-click scoring option (the click runs it — L2 Phase 0: canvas + initial score). Disappears once a score exists. No price chip (per the credit-surface cleanup). |
| 3.2 | Market + technical validation don't match the guidelines | 🟩 | Full **Validation Gate 1A ∥ 1B → 1C** alignment per the L2 walkthrough: 1A Market (problem, segment, 3+ competitors, TAM/SAM/SOM structured-first, differentiation, L1 watchers), 1B Technical (feasibility, dependencies, regulatory), 1C Problem-Solution Fit (5+ interviews, verbatim pain, **new WTP-signal check**) — **1C is locked until every 1A+1B check passes** (lock shown on the spine, agent won't push interviews early, skill refuses to run). New `customer-interviews` skill (EN+IT) generates the PSF interview kit. **Watchers now auto-propose when Phase 1 opens** ("i watcher L1 si attivano qui") — approve-first, never auto-activated. Verified: e2e script proves the whole pipeline live, 18/18 assertions (lock → proposals → approve → unlock → interviews → stage done). Loop 1 machinery (WTP ≥30% trigger, iteration caps) is the next chunk; the WTP check captures the data it will need. |
| 3.3 | Reshape revises in chat but not in Canvas + raw code block | ✅ | Two bugs: the model was leaking the reshaped canvas as a raw ```json block **outside** the artifact format (that's the code block you saw — it also meant nothing persisted), and even parsed reshapes were staged invisibly. Now: the parser **rescues** leaked canvas JSON into a real card, the prompt forbids the leak, an **approval card is always injected into chat** after a reshape, and the Canvas shows a dimmed "pending update" line under fields awaiting your yes. Core fields still commit only on your approval — that invariant is untouched (and we closed a bypass the review found: an unapproved TAM estimate can no longer green the "market sized" check). |
| 3.4 | Approving suggested watchers doesn't activate them | ✅ | The upload flow's suggestions were ephemeral — the Approve had nothing to execute, and the one path that did create watchers gave them a one-sentence scan prompt (~zero signals). Now every suggestion is persisted as a real proposal; checking it routes through the real executor which builds the **full scan prompt**; unchecked ones stay visible as "Proposed" in the Watchers tab; a rejected proposal is never re-asked on re-upload. Verified in the e2e: approve → active monitor with a real multi-section prompt. |
| 4 | Graph better but "contesto" mixes 4 categories | ✅ | Rebuilt to your mockup: **12 satellites** — FORNITORI, CLIENTI, HR E COLLABS, CONCORRENZA, BUSINESS ESSENTIALS, **PRODOTTO**, BRANDING, PARTNER, TREND TECH, INVESTITORI, GTM, TREND DI MERCATO — around the central startup node. All satellites always visible (empty ones as dashed ghosts), **click a category (label or area) → zoomed sub-view** with breadcrumb back, click a node → detail panel. "Contesto" is gone: tech trends, market trends, product features and business-model items each have their own home. Populated from chat, doc-upload, hiring signals, and your canvas's business fields (backfill script available for existing projects). Live-validated in Italian: all 12 labels, drill-down, ghosts, empty-state hint. ❓ One call to confirm: we added **PRODOTTO** as a 12th satellite for product features/metrics (your mockup had 11; your complaint listed product features) — easy to fold into Business Essentials if you prefer. |
| 5 | Remove the "input" part (to-dos + assumptions to accept)? | ❓ | Assumption-review cards were **already removed from the Inbox** in the June consolidation, and Home has no assumptions panel — we couldn't find a surface matching the description. Can you point at where you're seeing it? (Screenshot or page name is enough.) Assumptions still exist in the background feeding the stage checks — that stays either way. |
| — | UX reflections (personalized roadmaps per project type; free-conversation mode) | 💬 | Both good directions, deliberately not bundled into this fix batch. Note the new 1A/1B/1C track structure is exactly the substrate a per-project-type "corrected flow" would plug into (e.g. a B2G project could get extra 1B regulatory checks and a procurement track). Free-conversation + "decide where the output lands" (knowledge / workflow / separate) deserves its own design pass — worth a dedicated session. |

## 2. Verification

- `tsc --noEmit` clean; **vitest 224 passed / 0 failed** (50 new tests: gate tracks + 1C lock, approval-gated market sizing, graph taxonomy exhaustiveness, canvas-JSON rescue, Italian parsing fixtures).
- **New e2e** `scripts/e2e-validation-gate.mts`: seeds a Stage-1-complete project against a live server, proves lock → watcher proposals (idempotent) → approve → activate → 1A/1B green → 1C unlock → interviews close the gate → Stage 2 done. **PASS 18/18.**
- **Playwright browser QA on an Italian project**: graph (12 satellites, drill-down, ghosts, node panel), Home, error-recovery — all pass. One bug found & fixed during QA: category **labels** weren't clickable (only the colored area was); founders click the name, so labels now drill in too.
- Adversarial 3-lens review of the whole diff (correctness / product invariants / plan completeness): 18 findings triaged, all confirmed ones fixed — including one blocker (the market-size check could green without founder approval).

## 3. Deployment

- PR **#204** (Stage 1 ↔ L2 Phase-0) merged first; migration **030** (`idea_canvas.channels`) applied to prod before deploy.
- PR **#206** merged to `main` (`fde18be`); deployed to **launchpad.sensefound.io** 2026-07-06 (Netlify `6a4ba8c3`). Prod smoke-checked.
- Optional post-deploy: `npx tsx scripts/backfill-business-essentials.ts --apply` populates the BUSINESS ESSENTIALS satellite for existing projects (dry-run by default).
