node_modules/
.next/
# Stale build output the deploy script parks aside (.next.stale-<epoch>/).
# Untracked and unignored, it was swept into a commit by `git add -A` on
# 2026-09-11 — 2756 build files onto main.
.next.stale-*/
data/
*.db
*.db-journal
*.db-wal
.env.local
.env
.vercel
.env*.local
.env*.production

# Local Netlify folder
.netlify

# Playwright MCP snapshots
.playwright-mcp/

# Supabase CLI artifacts
supabase/.temp/
supabase/supabase/
deno.lock

# Next.js auto-generated type declarations
next-env.d.ts

# TypeScript build cache
tsconfig.tsbuildinfo
.gstack/

# Throwaway verification / debug scripts at repo root
/*-tmp.mjs
/*-tmp.ts
/*-tmp.cjs

# QA screenshots taken during ad-hoc verification
/canvas-*.png
/spine-*.png
/e2e-piano-*.png
/memory-scroll-jump.png
/interconnect-verify.png

# Claude Code local agent config / worktree state (not shared)
# shared project settings + skills ARE tracked
.claude/*
!.claude/settings.json
!.claude/skills/

# staging env (secrets — never commit)
.env.staging
